#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int x, y, z;
    cin >> x >> y >> z;

    if (x > 12 || y > 12) cout << 1 << endl;
    else cout << 0 << endl;

    return 0;
}